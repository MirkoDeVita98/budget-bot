# Messages for rules.py
usage_setbudget: "Usage: /setbudget <amount> (/sb)"
parse_amount_error: "Couldn't parse amount. Example: /setbudget 2500"
set_budget_success: "✅ Set budget for {month}: {amount:.2f} {currency}"

# Generic messages for rule setters (shared across setdaily, setweekly, setmonthly, setyearly)
usage_rule: |
  Usage:
  1) /{cmd} <category> <amount> (/{abbr})
  2) /{cmd} <category> <name> <amount> [currency]
  Examples:
  /{cmd} "Food & Drinks" 15
  /{cmd} "Food & Drinks" "Lunch allowance" 25 EUR
parse_amount_error_rule: |
  Couldn't parse amount.
  Example: /{cmd} "Food & Drinks" "Lunch allowance" 25 EUR
rule_success_base: |
  ✅ {period_name} rule added: [{category}] {rule_name} = {amount:.2f} {currency}/{period_label}{extra}
rule_success_fx: |
  ✅ {period_name} rule added: [{category}] {rule_name}
  {amount:.2f} {currency}/{period_label} → {converted:.2f} {base_currency}/{period_label} (rate {rate:.6f}, {fx_date}){extra}

no_rules: "No rules yet."
rules_list_header: "📌 Rules:"
rules_list_footer: |
  
  📋 Legend:
  ☀️  = Daily
  📆 = Weekly
  📅 = Monthly
  📊 = Yearly
  
  Delete one with: /delrule <id>
rules_list_item: "- ID {id}: [{category}] {name} — {amount:.2f} {currency} / {period}"

usage_delrule: "Usage: /delrule <id>"
rule_id_error: "Rule id must be an integer."
delrule_success: "🗑️ Rule deleted."
delrule_failure: "⚠️ Rule not found."